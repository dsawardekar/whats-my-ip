<{{ wrapper }} class="whats-my-ip">
  {% if ip %}
    <{{ wrapper }} class="ip"></{{ wrapper }}>
  {% endif %}

  {% if country %}
    <{{ wrapper }} class="country"></{{ wrapper }}>
  {% endif %}

  {% if coords %}
    <{{ wrapper }} class="coords"></{{ wrapper }}>
  {% endif %}
</{{ wrapper }}>
